# event-hub

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build event-hub` to build the library.

## Running unit tests

Run `nx test event-hub` to execute the unit tests via [Jest](https://jestjs.io).
