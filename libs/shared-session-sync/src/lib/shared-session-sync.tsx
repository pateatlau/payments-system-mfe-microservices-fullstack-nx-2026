import styles from './shared-session-sync.module.css';

export function SharedSessionSync() {
  return (
    <div className={styles['container']}>
      <h1>Welcome to SharedSessionSync!</h1>
    </div>
  );
}

export default SharedSessionSync;
