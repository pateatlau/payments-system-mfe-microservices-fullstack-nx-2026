# shared-event-bus

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-event-bus` to build the library.

## Running unit tests

Run `nx test shared-event-bus` to execute the unit tests via [Jest](https://jestjs.io).
