# shared-auth-store

This library was generated with [Nx](https://nx.dev).

## Building

Run `nx build shared-auth-store` to build the library.

## Running unit tests

Run `nx test shared-auth-store` to execute the unit tests via [Vitest](https://vitest.dev/).
